# Prism MVP Implementation Plan

## 목적

이 계획은 한 번에 모든 기능을 만드는 일정표가 아니다. 사용자가 실제로 투표하고 댓글을 읽고 특정 의견으로 생각을 바꾸는 핵심 루프를 가장 빠르게 검증하기 위한 수직 슬라이스 계획이다.

## 작업 그룹 0. 프로젝트 기반

### 범위

- Git 저장소 초기화
- Composer PSR-4 구성
- Docker Compose 로컬 환경
- 환경변수 로딩
- PDO 연결
- 마이그레이션 실행기
- 최소 라우터와 오류 처리
- PHPUnit과 Playwright 기본 구성

### 완료 기준

- `docker compose up`으로 웹과 DB가 실행된다.
- `/health`가 앱과 DB 상태를 반환한다.
- 테스트 명령이 빈 상태에서도 정상 실행된다.
- 비밀값이 Git에 포함되지 않는다.

## 작업 그룹 1. 질문 탐색

### 범위

- 홈 피드
- 게시 상태의 질문 조회
- 질문 상세
- 실제 데이터가 없을 때의 빈 상태
- 시드 질문 10개

### 완료 기준

- 모바일에서 질문 카드가 읽기 쉽다.
- 각 질문은 고유 slug를 가진다.
- 비공개 질문은 직접 URL로도 노출되지 않는다.

## 작업 그룹 2. 익명 방문자와 투표

### 범위

- 안전한 방문자 쿠키
- 방문자 해시 저장
- 주제별 최초 투표
- 결과 공개
- 중복 요청 방지
- 투표 수 트랜잭션 갱신

### 완료 기준

- 투표 전 결과가 보이지 않는다.
- 같은 방문자가 같은 주제에 최초 투표를 두 번 만들 수 없다.
- API 재시도에도 카운트가 한 번만 증가한다.

## 작업 그룹 3. 댓글과 추천

### 범위

- 투표한 사용자만 댓글 작성
- A/B 댓글 분리
- 댓글 추천
- plain text 출력과 XSS 방어
- 기본 정렬: 설득 수 → 추천 수 → 최신순

### 완료 기준

- 댓글의 진영은 작성 당시 투표와 일치한다.
- 방문자당 댓글별 추천은 1회다.
- 스크립트 태그와 HTML이 실행되지 않는다.

## 작업 그룹 4. 입장 변경과 설득 마커

### 범위

- 반대 진영 댓글에서 입장 변경 CTA
- 방문자당 주제별 변경 1회
- 변경 이벤트 append-only 저장
- 특정 댓글 attribution
- 댓글별 설득 수 표시
- 주제별 생각이 바뀐 고유 방문자 수

### 완료 기준

- 같은 진영 댓글로는 변경할 수 없다.
- 이미 변경한 방문자는 다시 변경할 수 없다.
- API 재시도에도 설득 수가 중복 증가하지 않는다.
- 설득 수치는 실제 attribution 이벤트와 일치한다.

## 작업 그룹 5. 신고와 최소 운영도구

### 범위

- 댓글 신고
- 신고 누적 시 임시 접기
- 관리자 로그인
- 질문 생성과 수정과 공개 상태 관리
- 댓글 숨김과 복구

### 완료 기준

- 신고만으로 콘텐츠가 영구 삭제되지 않는다.
- 관리자 비밀번호는 안전하게 해시된다.
- 관리자 경로에는 세션 보안과 CSRF가 적용된다.

## 작업 그룹 6. 공유와 분석 연결점

### 범위

- Open Graph 메타
- URL 공유
- GA4 이벤트 호출 인터페이스
- 개인정보 최소화
- 실제 수치 기반의 점진적 표시

### 완료 기준

- 공유 링크에서 제목과 선택지가 올바르게 보인다.
- 추적 차단 환경에서도 핵심 기능은 정상 동작한다.
- 숫자 0을 가짜 숫자로 대체하지 않는다.

## 작업 그룹 7. 품질과 배포

### 범위

- 핵심 PHPUnit 테스트
- Playwright E2E
- 모바일 접근성 점검
- 성능 점검
- 백업과 복구 문서
- Oracle Cloud 배포 문서
- Cloudflare 연결 문서

### 완료 기준

- `docs/15_ACCEPTANCE_CRITERIA.md` 전 항목이 통과한다.
- 390×844에서 핵심 루프를 막힘없이 완료한다.
- 운영 DB 백업과 복구를 한 번 실제로 검증한다.

## 첫 배포 전 중단 기준

다음 중 하나가 해결되지 않으면 공개 배포를 중단한다.

- 중복 투표 또는 중복 설득 수 발생
- 댓글 XSS 가능성
- 관리자 인증 우회
- 개인정보 또는 비밀키 노출
- 모바일에서 투표나 댓글 입력이 불가능함
- 실제 데이터와 화면 수치 불일치

## 이후 버전 후보

아래는 MVP 검증 후에만 검토한다.

- 댓글 답글
- 사용자가 만든 질문
- 계정과 프로필
- 30초 폴링 기반 활동 신호
- AI 의견 요약
- 설득왕 배지
- 광고와 구독
- 브랜드 스폰서드 질문
- B2B 인사이트 리포트
